# semantic leaning of pices of code with LLM

This package allows you to learn the semantics and functionalities of pieces of code provides

## Installation

Use the following command to install the package:

```bash
pip install .